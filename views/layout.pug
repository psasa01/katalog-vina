<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title= `${title} | ${h.siteName}`

    body
        block header    
            h1 header

        block content

        block scripts
            script(src="/dist/kolekcija-vina.bundle.js")